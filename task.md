# task.md

## Цель
Реализовать доработки Full UI platform для YASN: усилить UI/backend контракт, расширить schema до machine-readable формата, закрыть ключевые дыры UX и надежности, добавить проверки.

## План
- [x] Создать task.md и зафиксировать план.
- [ ] Реализовать schema v2 в native backend (structured type model + UI hints + backward compatibility).
- [ ] Ограничить UI API-экспозицию функций (скрыть внутренние/служебные и main, поддержать явный public API через экспорт).
- [ ] Добавить backend-валидацию аргументов `/call` по schema (до вызова VM).
- [ ] Доработать JS SDK (`@yasn/ui-sdk`) под schema v2 и улучшить обработку ошибок.
- [ ] Исправить React hooks в SDK (устранить повторные автозапросы/нестабильные зависимости).
- [ ] Доработать `@yasn/ui-kit` для typed form generation по schema v2.
- [ ] Добавить тесты (native + js) для UI schema/контракта/SDK.
- [ ] Обновить документацию UI-контракта и CLI addendum под новые возможности.
- [ ] Прогнать обязательные проверки: build Release + `yasn test` + дополнительные таргетные проверки.
- [ ] Подготовить итог и удалить task.md.

## Рабочий контекст
- Не ломать публичные CLI команды и pipeline компиляции/исполнения.
- Сохранить совместимость ответа API `/schema` для текущих клиентов, добавляя новые поля без удаления старых.
- Изменения минимально-целевая по связанным файлам (`Core/`, `Server/`, `packages/ui-*`, `docs/`).

## Текущий этап
Проектирую и реализую schema v2 в `native/yasn-native/Core/FunctionSchema.cs`, чтобы от неё зависели backend и UI-пакеты.
