# task.md

## Цель задачи
- Устранить повторяющуюся ошибку `repository ... .wiki.git not found` в workflow синхронизации Wiki и добавить более точную диагностику.

## Пошаговый план
- [ ] Обновить авторизацию в `.github/workflows/sync-wiki.yml` на схему `actor:token`.
- [ ] Добавить preflight `git ls-remote` для `.wiki.git` с явной подсказкой про создание первой страницы Wiki.
- [ ] Обновить docs с шагом про первичную инициализацию Wiki.
- [ ] Проверить дифф/поиск и выполнить минимальную проверку по AGENTS.
- [ ] Обновить итог в `task.md` и удалить `task.md`.

## Рабочий контекст
- Wiki — отдельный git-репозиторий `<repo>.wiki.git`.
- При неинициализированной wiki или недоступном токене GitHub может отвечать 404 `Repository not found`.
- Изменения должны быть минимальными и точечными.

## Текущий этап
- Вношу правки в `sync-wiki.yml` для корректной авторизации и более явной диагностики.
