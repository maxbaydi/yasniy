функция внутренняя_база(n: Цел) -> Цел:
    если n <= 1:
        вернуть 1
    вернуть n * внутренняя_база(n - 1)

функция двойной_факториал(n: Цел) -> Цел:
    вернуть внутренняя_база(n) + внутренняя_база(n)
