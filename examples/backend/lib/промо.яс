экспорт функция скидка_процент(код: Строка, сумма: Цел) -> Цел:
    если код == "SALE10" и сумма >= 500:
        вернуть 10
    если код == "VIP15" и сумма >= 1000:
        вернуть 15
    если код == "MEGA20" и сумма >= 2000:
        вернуть 20
    вернуть 0

экспорт функция бесплатная_доставка_по_коду(код: Строка) -> Лог:
    если код == "FREESHIP":
        вернуть истина
    вернуть ложь

экспорт функция описание_промо(код: Строка) -> Строка:
    если код == "SALE10":
        вернуть "SALE10: скидка 10% от 500"
    если код == "VIP15":
        вернуть "VIP15: скидка 15% от 1000"
    если код == "MEGA20":
        вернуть "MEGA20: скидка 20% от 2000"
    если код == "FREESHIP":
        вернуть "FREESHIP: бесплатная доставка"
    вернуть "Промокод не найден"
