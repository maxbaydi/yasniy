подключить "lib/математика.яс"
из "lib/ошибки.яс" подключить ошибка_подключения

функция main() -> Пусто:
    печать("2 + 3 =", сумма(2, 3))
    печать("8 - 5 =", разность(8, 5))
    печать("5! =", факториал(5))
    ошибка_подключения("демо сообщения")
    вернуть пусто
